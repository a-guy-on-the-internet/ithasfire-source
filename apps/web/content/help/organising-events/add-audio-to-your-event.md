---
title: "Add audio to your event description"
description: "Embed an audio clip in your event body and choose whether listeners can download it."
category: "organising-events"
order: 13
tags: ["events", "organiser", "audio", "media", "builder"]
related:
  [
    "organising-events/create-an-event",
    "organising-events/embed-widgets",
  ]
---

You can embed an audio clip — a track, a set recording, a spoken intro — directly in your event's body content. Listeners get an inline player on the event page. You decide, per clip, whether they can also download the file.

## Before you start

- You need to be editing an event in the builder at **/admin/{your-slug}/events**.
- Have the audio file ready. Supported formats are MP3, M4A, WAV, OGG, AAC, FLAC, and WebM.

## Add an audio clip

1. Open your event in the builder and scroll to **Details** (or jump there from the **Build** group in the side rail).
2. In the body content editor, place your cursor where you want the clip.
3. Click the **Insert Audio** button (the music-note icon) in the editor toolbar.
4. Choose a file. It uploads and embeds a player where your cursor was.

After upload, the clip is processed for smooth streaming. While that happens the block shows a processing state; once it's ready the player is fully playable. Click **Preview** in the builder header to open the event page in a new tab and hear it exactly as buyers will.

:::tip
Save as you go. The builder does not auto-save, and audio uploads on a slow connection are worth protecting.
:::

## Let listeners download the clip

By default a clip is **stream-only** — listeners can play it but not save the file.

To allow downloads, hover the audio block in the builder's **Details** section and turn on **Allow download**. When it's on, the block shows **Downloads allowed** and the player gains a **Download audio** button — both in your preview and on the live event page.

Turn the toggle off again at any time to remove the download button. The setting is per clip, so you can allow downloads on one track and not another in the same description.

:::warning
Allowing downloads lets anyone viewing the event page save the original audio file. Only enable it for audio you have the rights to distribute.
:::

## What can go wrong

- **No download button appears** even with the toggle on — the clip is still processing. Wait for processing to finish, then open the preview again.
- **The file opens in a new tab instead of saving** — some browsers stream rather than download depending on the file. The audio is still fully accessible; listeners can save it from the player's own controls.

## Related

Audio is one part of the body content you set when you [create an event](/help/organising-events/create-an-event). To promote the event elsewhere, see [embed widgets](/help/organising-events/embed-widgets).
